# SLOVNYK

# СЛОВНИК — курсова робота SQLite + SQLAlchemy 
# Cловники, пошук, звіти, слова та тлумачення у базі даних. 
# Файли тільки для експорту/імпорту JSON (папки export та input).



Версія: **2.54**

Консольний застосунок (CLI) для зберігання словників різними мовами та пошуку тлумачень.
Дані зберігаю у **SQLite** через **SQLAlchemy (ORM)**. Файли використовую лише для **експорту/імпорту JSON**.

## ✅ Можливості
- 📚 Словники (CRUD): створення/перегляд/редагування/видалення.
- 📝 Слова і тлумачення (CRUD): кілька тлумачень для слова, заборона видалення останнього тлумачення.
- 🔎 Пошук тлумачень за словом або фразою.
- 📤 Експорт у JSON (папка `export/`).
- 📥 Імпорт з JSON у базу (папка `input/`).

## 🧱 Структура
- `main.py` — старт программи. виклик головного меню
- `data/` — база `dictionary_obj` (створюється автоматично).
- `input/` — JSON для імпорту (є демо).
- `export/` — результати експорту (JSON).
- `slovnyk/`
          ├── __init__.py
          ├── config.py  - все, що є “налаштуванням”
          ├── db.py - все, що стосується БД
          ├── models.py - ORM-моделі SQLAlchemy
          ├── ui.py - консоль
          ├── services.py - -логіка CRUD (створити/редагувати/видалити словник/слово/тлумачення, пошук)
          ├── reports.py - звіти
          ├── io_json.py - імпорт/експорт JSON
          └── menus.py - меню та маршрути (словники, слова, тлумачення)

## ⚙️ Вимоги
- Python 3.11+
- SQLAlchemy 2.x

## ▶️ Запуск
```bash
pip install -r requirements.txt
python main.py
```

## 📥 Формат JSON для імпорту
Файл — масив словників:
```json
[
  {
    "nazva": "Демо англійсько-український словник 1",
    "typ": "en-uk",
    "slova": [
      {
        "slovo": "word_1",
        "tlumachennia": ["переклад 1", "пояснення 1"]
      }
    ]
  }
]
```

## 🧪 Швидка демонстрація
1) Меню **Звіти / експорт / імпорт** → **Імпорт з JSON у базу даних** → Enter (імпорт демо).
2) Меню **Словники** → перегляд списку.
3) Меню **Слова і тлумачення** → список слів/деталі/додавання тлумачення.
4) Меню **Пошук** → пошук у вибраному словнику.
5) Меню **Звіти / експорт / імпорт** → експорт у JSON та перевірка файлів у `export/`.

